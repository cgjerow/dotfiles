##//extend-cli begin

# Path to your oh-my-zsh installation.
export EXTEND_CLI="/Users/connorjerow/.extend-cli"

# Setup the alias
alias ec="yarn --silent --cwd $EXTEND_CLI run extend-cli"

# User configuration
export AWS_DEFAULT_PROFILE=partnerssandbox

##//extend-cli end
